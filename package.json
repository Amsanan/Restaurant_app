{
  "name": "restaurant-app",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:server": "pnpm --filter @restaurant/server dev",
    "dev:web": "pnpm --filter @restaurant/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
